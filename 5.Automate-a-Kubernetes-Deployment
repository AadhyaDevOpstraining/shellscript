#!/bin/bash
# Apply Kubernetes configuration

KUBE_CONFIG="/path/to/kubeconfig"
kubectl --kubeconfig="$KUBE_CONFIG" apply -f /path/to/deployment.yaml

echo "Kubernetes deployment applied successfully!"